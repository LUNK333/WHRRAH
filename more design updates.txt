Design updates:

Add ability to set number of decimal points for numeric displays, bar graph numbers
Change numeric display layout to:

 XXX
Label

Adjustable font size of XXX and Label

Remove the green outline boxes around widgets (lap timer, track map, numeric display have these rn)

Have data wizard cache the loaded matches when its closed so I don't have re run the matcher every time I open it
Add lap counts, best lap/ time to wizard table like the lap selector

Move export video button to the video groupbox on LH column

Bugs:
When a data/video pair are loaded the offset is zero after clipping the data length to the video (good), however if I proceed to select a lap a large offset gets applied in the video offset
Seeking gets messed up if I try to seek backwards after doing this
I also get an error message if I try to add padding after selecting a lap now
Test case:
load session 1221/ video 278 pair (zero offset with all laps selected, correct)
select lap 6, offset is now -911.74s
